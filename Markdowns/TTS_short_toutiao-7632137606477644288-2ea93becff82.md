<!-- VIDEO_ID: toutiao-7632137606477644288-2ea93becff82 -->
# Claude Code Skills 完全指南：安装、创建与使用

## 1. 内容概览
本视频详细讲解了 Claude Code 的扩展机制——Skills（技能包）。内容包括 Skills 的定义、核心特点、与 Plugins 的区别、三种安装方式、如何从零创建自定义 Skill、自动触发与手动触发的逻辑、以及管理已安装 Skills 的方法。最后推荐了获取优质 Skills 的三个渠道。

## 2. 时间线

### [00:00:00 - 00:01:00] Skills 简介与作用
Claude Code 受欢迎不仅因为模型强，还因为其丰富的 Skills 生态。Skill 是 2025 年 10 月推出的扩展机制，本质是一个 Markdown 文件，包含针对特定任务的提示词和工作流程。例如安装 PPT 技能后，只需说“帮我做个 PPT”，Code 就知道如何排版和走流程。

### [00:01:00 - 00:02:00] Skills 的核心特点
Skill 让 Claude Code 从通用助手变为领域专家。三个关键特点：
1. **可复用**：写好一次，每次对话都能用。
2. **可分享**：别人写好的装上就能用。
3. **自动触发**：Code 根据需求自动匹配，也可用斜杠命令手动触发。
本质是将专家经验打包成可复用模块。

### [00:02:00 - 00:03:00] Skills 与 Plugins 的区别
*   **Skill**：小模块，核心文件是 `skill.md`，写用途、提示词规则和工作流程。旁边可带参考文档、示例或脚本。
*   **Plugin**：完整的安装包（大目录），包含一个 `plugin.json` 描述插件，内部可包含多个 Skill 和其他组件。
总结：Skill 是能力模块，Plugin 是打包这些模块的容器。

### [00:03:00 - 00:04:00] Skills 存放位置与手动安装
两个存放目录：
1.  **项目级**：项目根目录下的 `.claude/skills` 文件夹，仅当前项目生效（适合专用规范）。
2.  **用户级**：Home 目录下的 `.claude/skills` 文件夹，所有项目可用（适合通用技能如 PPT、公众号排版）。
**手动安装**：直接下载别人的 Skill 目录，放入上述任一目录即可。

### [00:04:00 - 00:05:00] 工具安装与插件安装
**第三方工具一键安装**：
*   常见命令类似 `npx skills add ...`。
*   `npm`、`pnpm`、`bun` 效果一样，根据电脑安装的包管理器选择。
**插件安装**：
*   在对话框使用斜杠命令 `/plugins`。
*   官方市场直接执行，第三方市场需先添加市场再安装。安装后 Skill 自动生效。

### [00:05:00 - 00:06:00] 自定义 Skill 的结构
一个 Skill 是一个文件夹，必须包含 `skill.md`。
*   **可选文件**：参考文档、示例模板、Python/JS 脚本等。简单 Skill 只有一个 `skill.md` 即可。
*   **复杂 Skill**：将详细内容拆分到配套文件中，Code 会自动读取。

### [00:06:00 - 00:07:00] Skill.md 编写规范
分为两部分：
1.  **元信息（YAML Frontmatter）**：
    *   `name`：技能名字（斜杠命令名）。
    *   `description`：**非常重要**，描述技能用途，Code 根据此判断是否自动触发。
2.  **正文**：具体指令，分几步走、做什么、格式注意什么。
*   **快速创建**：直接在对话告诉 Code 创建，或使用内置 Skill `/skills` 引导创建和优化。

### [00:07:00 - 00:08:00] 自动触发与手动触发
*   **自动触发**：Code 读取所有已安装 Skill 的 `description`，当用户输入与描述匹配时自动调用。Description 写得越好，触发越准。
*   **手动触发**：在对话框输入 `/技能名`。
*   **控制参数**：
    *   `should_auto_trigger: false`：禁止自动触发（用于部署等有副作用的操作）。
    *   `should_list_in_slash_commands: false`：不显示在斜杠菜单中，但 Code 觉得需要时仍会自动调用。

### [00:08:00 - 00:09:00] 查看与管理 Skills
*   **查看**：
    *   `/skills`：列出所有可用技能和命令，可筛选。
    *   `/slash`：专门列出已安装的，更清晰。
*   **停用/删除**：
    *   **手动安装**：修改 `skill.md` 元信息禁用自动触发，或直接删除文件夹。
    *   **插件安装**：使用 `/plugins` 打开管理页面操作。

### [00:09:00 - 00:09:59] 寻找更多 Skills 的渠道
1.  **官方插件市场**：对话框输入 `/plugins`，有 Anthropic 官方和社区认证，质量有保证。
2.  **第三方分享网站**：收录大量社区贡献，有分类搜索和排行榜。
3.  **GitHub/Gitee**：搜索开源 Skill 和 Plugin 项目，高质量资源常首发于此。

## 3. 关键事实
*   **发布时间**：Skills 于 2025 年 10 月推出。
*   **核心文件**：`skill.md`。
*   **目录结构**：
    *   项目级：`.claude/skills`
    *   用户级：`~/.claude/skills`
*   **触发逻辑**：基于 `description` 字段进行语义匹配。
*   **安装命令**：支持 `npx`、`pnpm`、`bun` 等包管理器。

## 4. 可检索细节
*   **元信息字段**：`name`, `description`, `should_auto_trigger`, `should_list_in_slash_commands`。
*   **内置命令**：
    *   `/skills`：查看和管理技能。
    *   `/plugins`：管理插件。
    *   `/slash`：列出斜杠命令。
*   **相关工具/网站**：
    *   `skillsmp`
    *   `skills.sh`
    *   `agentskill.sh`
    *   `Lobehub Skills`

## 5. 视觉证据索引
*   Frame [00:00:38]: 展示 Skill 理解为 "技能包" 的概念图。
*   Frame [00:01:10]: 列出 Skill 关键特点：重复使用。
*   Frame [00:01:54]: 展示 Skill 文件结构，高亮 `SKILL.md` 为主入口。
*   Frame [00:04:09]: 展示安装命令对比，说明 npx/pnpm/bun 等价。
*   Frame [00:06:39]: 标题页 "3 Skill使用方式?"。
*   Frame [00:08:33]: 展示在文件管理器中直接删除 Skill 文件夹。

## 6. 完整逐字稿
[00:00:00 - 00:01:00] cloud code之所以受欢迎，不仅是因为cloud系列模型很强，还因为它打造了一个丰富的ski生态。但我发现现在还有很多人不知道怎么操作。所以本期视频我将手把手教大家在cloud code中安装和使用s内容包是什么怎样安装和创建样触发和使用ki以及去哪找更多更好用的ski等等，全程无广无废话。希望大家在受伤的同时能点赞支持一下。如果关注一下，那就更好了。感谢各位首先我们先简单说一下是什么。以及它有什么作用是在2025年10月份推出的一个cloud code的扩展机制，你可以把它理解成是cloud code的技能包，本质上就是一个markdown文件，里面写了一套针对特定任务的提示词和工作流程。比如说你装了一个做P以你只需要跟他说一句，帮我做个PPTud code就知道该怎么排版，用什么格式走什么流程。比如说你装了一个公众号排版的就能够直接把转换成公众号可以直接发布的。😊
[00:01:00 - 00:02:00] ML格式简单来说，skill就是让cloud code从一个通用的AI助手变成一个在某个领域特别专业的助手。它有三个关键特点。第一，它是可复用的，写好一次以后每次对话都能够用。第二，它是可以分享的。别人写好的skill，你装上就能够用。第三，它是自动触发的。你不需要手动指定用哪个skill cloudud code会根据你的需求，自动匹配合适的skill。当然你也可以用斜杠命令的方式手动触发。所以skill本质上就是一种把专家经验打包成可复用模块的方式，理解了这个后面的内容就比较好懂了。在开始安装之前，我先说一个很多新手容易搞混的点，那就是skill和插件的区别。如果你从文件结构上来看，这个事情其实很好理解。skill本质上就是一个单独的能力目录，它最核心的文件叫skill点MD里面写的是这个ski的用途，提示词规则和工作流程。如果有需要，它旁边也可以带一些设立文件参考资料，甚至是脚本。所以你可以把ski。😊
[00:02:00 - 00:03:00] 解成一个专门解决某一个任务的小模块。那pl是什么呢？如果说ski是小模块，那pl就更像是一个完整的安装包，它本身是一个更大的目录，里面通常会有一个pl用来描述这个插件是什么之它里面还可以继续包含多个也可以包含别的组件，这些也就是说pl不是某一个能力本身，而是把多个能力和配置，一起打包成一个可以安装分发复用的整体装进去之后，里面的多个skill和相关配置就能够一起生效。用一句话总结就是skill是一种能力模块。pl是打包这些能力模块和其他组件的容器。理解了这个我们再来看一下具体怎么安装。总结下来就有两种方式，一种是直接用别人写好的一种是自己从创建我们分别来先来看一下如何安装别人的首先我们要知道ski文件应该放在哪里 code有两个目录可以放一个是项目级目录就是你项目跟目录下的。😊
[00:03:00 - 00:04:00] skills文件夹放在这里的s只在当前项目里面生效，适合跟项目强相关的技能。比如说这个项目专用的代码规范部署流程之类的。第二个是用户级目录，就是你home目录下的 cloud文件夹放在这里的ki在所有项目里面都可以用适合那些通用的技能。PT文档排版公众号这些与具体项目无关的。知道了放哪里，我们再来看一下怎么安装，一共有三种方式，第一种方式是手动复制这是最简单直接的方式如果别人分享的就是一个skill目录，你直接下载下来，放在刚才说的目录里面就可以了。你在上看到了一个s把它的文件夹下载下来，丢到了项目下的 cloud文件夹下面次打开cloud code就会自动识别到第二种方式是用第三方工具一键安。如果你经常逛这类分享网站，你会发现他们通常会提供一行安装命令类似于这样的这个命令本质上就是帮你自动下载文件，然放到正确的目录里。😊
[00:04:00 - 00:05:00] 省得你自己去克隆仓库手动复制。另外你可能还会看到BNX或者是PNPMDX开头的命令，不用困惑，他们和NX是一回事，只是用了不同的包管理器而已。你电脑上装了哪个就用哪个效果完全一样。第三种方式是通过插件来安装。如果你要装的不是单个s，而是一个完整的插件，那就在cloud code的对话框里面用斜杠命令来安装。如果插件是在官方市场里面直接执行这个命令就可以了。比如说安装suppower使用的就是这个命令。如果插件在第三方市场，你首先需要添加这个市场，然后再用这个命令进行安装。安装完之后，插件里的ski就会自动生效。你不需要再做任何额外的操作，以上三种方式，不管你用哪一种，最终的效果都是一样的。那就是把s文件放到了正确的位置，然后cloud code就能够用了。知道了如何安装别人的s，下面我们再来看一下怎样创建自己的s。过再讲如何操作之前，你首先要知道一个s长什么样子。一个ski就是一个文件夹里面必须有。
[00:05:00 - 00:06:00] 一个ski点MD文件，这个是核心。除此之外，你还可以放一些配套文件，比如说参考文档视例模板，甚至是python或者是bs脚本，这些配套文件不是必须的。简单的ski一个ski点M就够了。但如果你的s比较复杂，就可以把详细的内容拆到这些文件里 code在需要的时候自动读取那ski点M文件应该怎么写的？它分为两个部分一个部分是文件开头的原信息用两行横杠包起来里面写的是这个ski的基本信息，最常用的有这么几个字段name就是技能的名字，也就是斜杠命令的名字descript就是描述告cloud code这个技能是干嘛的，什么时候用它这个描述非常重要。因为ud code就是根据这个描述来判断要不要自动触发这个第二部分就是正文的就你希望ud code怎么做的具体指令分几步走做什么格式意什么事项平时写提示词是一样的，只不过是写在了文件里面举个最简单的例子，比如说你想创建一个帮你写。😊
[00:06:00 - 00:07:00] 提交信息的ski，那它的skill点MD大概就是长这样，然后把这个文件放到文件夹里面，然后再把这个文件夹放到你项目或者是用户目录下的点cloud skills文件夹下面就装好了。下次你让cloud code帮你写提交信息，它就会自动用这个ski。当如果你觉得自己手写太麻烦。还有两种更加轻松的方式。第一种是直接在对话里面直接告诉cloud code如说帮我创建一个写commit message的，它会自动帮你生成文件放到正确的位置。第二种是用skillc这个内置技能，它会引导你一步一步完成ski的创建，还能帮你做测试和优化，特别适合刚开始接触ski的新手装好之后怎么用呢？其实有两种方式自动触发和手动触发。来看一下自动触发。大多数时你根本不用操心用哪个skiud code会自己判断那它是怎么判断的呢？还记得我们刚才写skill点M时，里面有一个descript描述字段吗colud code会读取所有已安装ski的描述，当你说的话跟某个s描述匹配上了。😊
[00:07:00 - 00:08:00] 它就自动用上了。比如说你装了一个描述，写着生成规范的get提交信息的skill。但只要你说帮我写个comit message，cloud code就会自动触发这个sll不需要你自己指定，所以description写的好不好，直接决定了自动触发准不准。这也是为什么。我前面说这个字弹非常重要的原因。再来看一下手动触发。如果你明确知道要用哪个skill，也可以直接用斜杠命令来触发，在对话框里面输入斜杠技能名就可以了。比如说输入这个就会直接触发那个写提交信息的skill。有些skill你可能不想让colud code自动触发，比如说部署或者是发消息这种带有副作用的操作，让col code自己判断要不要部署，那也有点太吓人了。这个时候你可以在原信息里面加一行这个参数，加了之后，colud code就不会自动触发这个skill了。只有你手动输入斜杠命令才会执行。反过来，如果有一些skill你只想让cloud code在背后自己用，不需要出现在斜杠菜单里面，那就设置为这个参数，这样用户就看不到这个skill，但cloud code觉得有需要的。😊
[00:08:00 - 00:09:00] 还是会自动调用它。最后再说一下怎样查看和管理已经安装好的ski。如果你想看看自己装了哪些skill，有两种方式。第一种，在对话框里面输入一个斜杠col code会列出所有可用的ski和命令，你可以继续打字来筛选过滤。第二种直接输入斜杠命令，它会专门列出所有已安装的看起来更加清晰。如果你想停用或者是删除呢，这里要分两种情况。如果是你自己手动安装的skill想要停用的话，那可以去改ski点Md里面的原信息，比如说加上这个参数，禁止自动触发，想要彻底删除的话，可以直接把那个skill文件夹删掉就可以了。删完就没了，非常干净。如果是通过插件装的ki你就用插件的管理命令，在对话框里面输入斜杠会打开插件管理页面，在这里你可以看到所有已经安装的插件想停用哪个直接在界面里面操作就可以了。总之不管是触发还是管理 code都帮你做的非常简单了。中种情况下装好的ski就不用管了。学会了安装和使用之后，你肯定会想去哪找更多更。😊
[00:09:00 - 00:09:59] 这里我给大家推荐三个方向。第一个是使用官方的插件市场，在cloud code对话框里面输入斜杠pl就能够打开官方的插件市场里面有anropic官方和社区认证的插件质量有保证这是最推荐的渠道。二个是使用第三方的分享网站。比这些网站收录了大量社区贡献的分类搜索能够查排行榜和安量这些网站之专门出过一期视频细介绍过感兴趣的同学可以去看看。第三个渠道是直接在搜或者ud就能够找到大量开源的和插件项目多高质量的都是在首发的总ud生态已经非常丰富了。不你是做开发设计还是做内容创作都能够找到合适自己的以上就是本期视频的所有内容果你有一点点帮助别忘了点连加关我期再见。😊

<!-- END_VIDEO_ID: toutiao-7632137606477644288-2ea93becff82 -->
