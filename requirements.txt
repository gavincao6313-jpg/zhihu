google-genai>=1.0.0
faster-whisper>=1.0.0
numpy>=1.24.0
Pillow>=10.0.0
# AMD GPU (Vulkan) 加速: CMAKE_ARGS="-DWHISPER_VULKAN=ON" pip install whisper-cpp-python
# 并设置环境变量: WHISPER_BACKEND=vulkan
# 默认回退 faster-whisper (CPU)
